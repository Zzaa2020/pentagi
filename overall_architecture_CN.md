# 理解 Pentagi：它如何与 AI协同工作

欢迎来到 Pentagi 的幕后！本文档将解释 Pentagi 的构建方式，以及它如何巧妙地利用人工智能（AI）来理解您的请求并完成任务。我们将探讨其主要组成部分，它如何从头到尾处理一项任务，以及 AI 在其中扮演的不同“角色”。

## Pentagi：深入了解其内部结构

Pentagi 的构建旨在实现灵活性和随需应变的可扩展性，就像一组可以重新排列和添加的积木。它将不同的工作分开，以便于管理。您可以将其视为包含两个主要部分：您所看到并与之交互的部分（**前端**），以及执行工作的引擎（**后端**）。

### 前端：您的控制面板

前端是您作为用户与之交互的一切。您在此屏幕上告诉 Pentagi 该做什么，查看进展情况，并获取结果。当您给 Pentagi 分配一项任务时，前端会将您的指令发送到后端。

### 后端：引擎室

后端是施展魔法的地方。它接收您的指令并管理 AI 以完成工作。可以将其想象成一个拥有不同经理和专家的运营中心。

后端的关键部分包括：

*   **路由器（Router）**：将路由器视为**前台接待员**。当前端发来请求时，路由器会判断请求需要发送到哪里，并将其引导至正确的管理员。
*   **控制器（Controllers）**：这些是监督工作不同阶段的管理员。
    *   **FlowController（项目经理）**：这就像一个大型任务（称为“flow”或“流程”）的**项目经理**。它负责监督从开始到结束的整个工作。
    *   **TaskController（团队主管）**：对于工作的每个主要部分，FlowController 都会指派一个 TaskController。这个**团队主管**负责将该部分分解为更小、更具体的步骤。
    *   **SubtaskController（专家）**：每个具体步骤都由一个 SubtaskController 处理。这位**专家**专注于完成一个特定的操作，通常直接与 AI 合作。

### 关键支持组件：后端的工具包

为了完成工作，后端依赖于一些重要的辅助工具：

*   **AI Provider（通用 AI 翻译器）**：Pentagi 可能与不同的 AI 模型（例如来自 OpenAI 或 Anthropic 的不同“大脑”）协同工作。AI Provider 就像一个**通用翻译器**，使 Pentagi 能够与为任务选择的任何 AI 大脑进行清晰的沟通。
*   **Prompter（AI 的剧本作家）**：为了从 AI 获得最佳结果，您需要非常精确地向其提问或发出指令。Prompter 就像一位**AI 剧本作家**，它为 AI 创建这些精确的脚本（称为“prompts”或“提示”），确保 AI 根据当前步骤、可用工具和总体目标理解该做什么。
*   **ToolsExecutor（车间主管）**：有时，AI 需要在现实世界中执行操作，例如浏览网站、运行命令或检查文件。ToolsExecutor 就像一位**车间主管**。它拥有一套经批准的工具，AI 可以请求使用这些工具，并且它会确保这些工具得到正确和安全的使用。

这种具有明确角色和职责的设置使 Pentagi 易于适应和更新。既然我们已经了解了 Pentagi 的主要部分，接下来让我们看看当您给 Pentagi 分配一项任务时，它们是如何协同工作的。

## 用户触发任务的生命周期：从您的请求到结果

让我们逐步了解当您给 Pentagi 分配一项任务时（例如“扫描 example.com 的安全问题”）会发生什么。

1.  **您启动任务**：您在前端输入您的目标并点击“开始”。

2.  **接收指令**：前端将您的请求发送到后端的**路由器**（接待员），路由器将其视为一个新的主要任务（“流程”），并将其传递给 **FlowController**（项目经理）。

3.  **项目经理接管（FlowWorker 激活）**：
    *   **FlowController**（我们的项目经理）准备好一个 `FlowWorker` 来处理整个任务。
    *   此 `FlowWorker` 设置必要的支持：
        *   选择并配置 **AI Provider**（通用翻译器）。
        *   **Prompter**（AI 的剧本作家）准备为 AI 草拟指令。
        *   **ToolsExecutor**（车间主管）准备 AI 可能需要的工具集。

4.  **分解任务（任务创建与分解）**：
    *   `FlowWorker`（项目经理）将主要目标移交给由 **TaskController**（我们的团队主管）管理的 `TaskWorker`。
    *   `TaskWorker`（团队主管）在 **Prompter** 的帮助下，要求 **AI Provider** 扮演*规划者*的角色。AI 查看您的目标（“扫描 example.com”），并将其分解为一系列较小的步骤，例如：“1. 检查 example.com 上正在运行的服务”，“2. 查看这些服务是否存在已知问题”等。

5.  **逐步执行（子任务执行循环）**：
    *   `TaskWorker`（团队主管）逐个处理这些步骤（子任务）。
    *   对于每个步骤，它都会引入一个由 **SubtaskController**（我们的专家）管理的 `SubtaskWorker`。
    *   `SubtaskWorker`（专家）使用 **Prompter** 指示 **AI Provider** 扮演*执行者*的角色。AI 现在专注于这一个步骤。
    *   作为*执行者*的 AI 可能会决定需要一个工具。例如，要“检查正在运行的服务”，它可能会向 **ToolsExecutor**（车间主管）请求一个“网络扫描”工具。工具的结果将发送回 AI。
    *   **需要更多信息？**：如果 AI 意识到没有您的更多信息就无法完成该步骤，它会通知 `SubtaskWorker`。整个过程暂停，等待您的输入。

6.  **轮到您（处理用户输入）**：
    *   如果暂停，您通过前端提供所需的信息。
    *   此信息通过项目经理、团队主管和专家传回给等待中的 AI。
    *   有了新的信息，AI 会重新开始处理该步骤。

7.  **完成主要部分（任务完成与总结）**：
    *   一旦 `TaskWorker`（团队主管）下的所有步骤都完成，它会再次要求 **AI Provider** 扮演*总结者*的角色。
    *   AI 查看这些步骤中发生的一切，并为该部分工作撰写摘要（例如，“发现服务 A、B 和 C。服务 A 存在潜在问题。”）。

8.  **项目经理审查（流程继续）**：
    *   `FlowWorker`（项目经理）获取此摘要。
    *   根据整个任务的设置方式，它可能会向您显示这些结果，开始任务的另一个主要部分，请求您的下一步指令，或决定整个任务已完成。

这个过程使 Pentagi 能够通过分解复杂目标、在不同角色中使用 AI 并在需要时让您参与进来，从而处理复杂目标。生命周期展示了 AI 扮演的不同角色。让我们仔细看看这些特定的 AI 角色。

## AI 所扮演的角色：AI 戴上的不同帽子

在整个任务过程中，AI 在 **Prompter** 的引导下，通过 **AI Provider** 进行沟通，扮演着几个特定的角色，就像团队中的不同专家一样：

1.  **任务标题生成器（快速贴标者）**：
    *   **职责**：根据您的初始请求，为任务想出一个简洁明了的标题。
    *   **何时**：当新的 `TaskWorker`（团队主管）首次接到任务时。这有助于每个人跟踪事务。

2.  **规划/分解器（战略家）**：
    *   **职责**：分析赋予 `TaskWorker`（团队主管）的主要目标，并通过将其分解为更小、可管理的子任务来制定最佳策略。
    *   **何时**：当 `TaskWorker`（团队主管）需要创建分步计划时。

3.  **执行/代理（实践专家）**：
    *   **职责**：这是 AI 亲自动手，专注于由 `SubtaskWorker`（专家）管理的单个特定子任务。它会：
        *   理解该单个步骤需要完成什么。
        *   决定是否需要来自 **ToolsExecutor**（车间主管）的工具——例如运行命令或检查文件。
        *   使用工具并理解结果。
        *   根据结果思考下一步该做什么。
        *   判断是否拥有所需的一切，或者是否应该向您请求帮助。
    *   **何时**：每当 `SubtaskWorker`（专家）处理特定操作时。这是大部分“执行”工作发生的地方。

4.  **总结/提炼器（报告员）**：
    *   **职责**：在 `TaskWorker`（团队主管）监督其所有子任务完成后，此 AI 角色介入，查看所有单个结果、日志和产出。然后，它会为整个任务的成果撰写一份清晰、全面的摘要。
    *   **何时**：当 `TaskWorker`（团队主管）完成所有分配的步骤并需要报告最终结果时。

通过让 AI 在这些角色之间切换，Pentagi 可以在每个阶段有效地运用其智能——从规划工作到执行工作，最后到报告工作进展。系统的“管理者”（控制器和执行单元）确保 AI 获得适合特定角色的正确脚本（提示）。

---

从本质上讲，Pentagi 就像一个组织良好的团队。它将传统的软件组件（如项目经理、团队主管、专家和工具主管）与一个多功能的 AI 相结合，该 AI 可以扮演规划者、实践专家和报告员的角色。这种团队合作使 Pentagi 能够以智能和适应性强的方式处理复杂任务，始终将您的目标放在首位。
